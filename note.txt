盤のグリッドを基準とした座標→cellPos(column, row)
画面の解像度を基準とした座標→coordinate(x, y)

どちらも左上が小さい
マスを座標で指定する場合は左上を指定する

盤は通常縦に40、横に10ある

Table→Cell[][]で表したブロックの集合

PlaySceneに固有のメンバ変数や関数を下位から参照しない

コンフィグ
バッグの方式
スピンの方式
コンボテーブル
ボードの幅と高さ
ネクスト・ホールド
重力（可変）
ロックディレイ

CurrentMinoManager 現在のミノの座標や形
BoardControlState ゲーム的なコントロール状態
ControlOrder 操作の入力状態
BoardController ボード・ミノ・操作の状態を読み込み、時間を進める

セルの描画に関わる要素
・ブロックであるかどうか
・性質
・色
・スキン

リファクタリング
BoardController
config(ゲーム中に変化しない設定)を定義してGameContextから引っ張る
ViewController
作成時にスキンに応じてテクスチャを生成する
settingsはただのオブジェクトにする
scene.width scene.height 削除
cellSheetTextureにGhostなどのために複数のsourceImageを使う
ミノの形色＝番号対応は実行時に決められるようにし、文字と数字を使い分ける